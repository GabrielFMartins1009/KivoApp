<?xml version="1.0" encoding="utf-8" ?>
<Shell x:Class="KivoApp.AppShell"
       xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:KivoApp"
       Title="KivoApp">

    <!-- Flyout menu -->
    <FlyoutItem Title="Início" Icon="home.png">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Relatórios" Icon="chart.png">
        <ShellContent ContentTemplate="{DataTemplate local:RelatoriosPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Metas" Icon="target.png">
        <ShellContent ContentTemplate="{DataTemplate local:MetasPage}" />
    </FlyoutItem>

</Shell>
